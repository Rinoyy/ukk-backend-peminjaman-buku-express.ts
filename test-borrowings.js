const http = require('http');

async function test() {
  // We need an admin token. Let's look for a valid token or just hit the dev db.
  // Actually, wait, we can just edit the borrow.controller.ts temporarily to log the error to a file!
}
test();
